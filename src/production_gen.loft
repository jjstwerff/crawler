// GENERATED by tools/gen_bundles.loft -- DO NOT EDIT BY HAND.
// What each SETTLEMENT WORKSHOP makes, contributed by the "production" sections of the
// kind:"world" bundles. The engine owns the seven producer kinds and the rotation; a
// bundle owns the entries. Merged with the engine's own in catalog.loft. See BUNDLE.md.

// Entries a bundle adds for this producer (0 = none).
pub fn bundle_production_len(producer: text) -> integer {
  if producer == "alchemy" { return 1; }
  0
}

// The i-th bundle-contributed entry for this producer ("" = out of range).
pub fn bundle_production_key(producer: text, i: integer) -> text {
  if producer == "alchemy" {
    if i == 0 { return "naga_antivenin"; }
  }
  ""
}
