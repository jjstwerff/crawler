// GENERATED by tools/gen_bundles.loft — DO NOT EDIT BY HAND.
// Forwards the stencils()/placement() of the kind:"world" bundles to the engine placer
// (sim.loft). The room templates + placement rules live in the bundle; see BUNDLE.md.
use worldtypes;
use ds_stencils;
use ds_placement;

pub fn world_stencils() -> vector<RoomStencil> {
  t: vector<RoomStencil> = [];
  s_ds_stencils = ds_stencils::room_stencils();
  for i in 0..len(s_ds_stencils) { if i < len(s_ds_stencils) { t += [s_ds_stencils[i]]; } }
  t
}

pub fn world_has_placement() -> integer { 1 }
pub fn world_placement() -> Placement { ds_placement::place_rules() }
