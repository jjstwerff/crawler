// GENERATED by tools/gen_bundles.loft — DO NOT EDIT BY HAND.
// Aggregates the definition sections (enemies()/items()) of the kind:"world" bundles so the
// catalog merge (catalog.loft) can fold them into the game tables. The content lives in the
// bundle (bundles/<name>/...); this is just one `use` + concat per module. See BUNDLE.md.
use monsters;
use items;
use ds_enemies;
use ds_items;

pub fn bundle_monsters() -> vector<MonsterDef> {
  t: vector<MonsterDef> = [];
  s_ds_enemies = ds_enemies::monster_defs();
  for i in 0..len(s_ds_enemies) { if i < len(s_ds_enemies) { t += [s_ds_enemies[i]]; } }
  t
}

pub fn bundle_items() -> vector<ItemDef> {
  t: vector<ItemDef> = [];
  s_ds_items = ds_items::item_defs();
  for i in 0..len(s_ds_items) { if i < len(s_ds_items) { t += [s_ds_items[i]]; } }
  t
}
