# Featured as a loft build example in the "state of the loft distribution" overview
# (loft-lang/loft @PLN112). Edit here — this repo owns its showcase blurb.
[[showcase]]
demonstrates = "a hex roguelike with a renderer-agnostic kernel — the first cross-game consumer of the hex_* world libraries"
