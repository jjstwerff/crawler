# loft build caches / artifacts — ignore everything under .loft/ EXCEPT the
# committed, agent-readable dep API stubs (loft#362)
**/.loft/*
!**/.loft/api/
*.wasm
native-auto/

# generated game builds
/story.html
/story.png

# editor / OS noise
*.swp
*~
.DS_Store
tools/__pycache__/
tools/_overland/
tools/_triangle/
.story_next
